const Duplicat = () => {
    return ( 
        <>

{/* //   const [title, setTitle] = useState("Title");
//   const [showInputEle, setShowInputEle] = useState(false); */}



{/* //   const handleDoubleClick = () => { */}
    {/* setShowInputEle(true); */}
   {/* <Box display="flex" flexDirection="column" mt="50px">
        <Box display="flex" alignItems="center" mt="10px">
          <img alt="drop" src={droplogo} style={{ marginRight: '10px' }} />
          {showInputEle ? (
            <input
              type="text"
              value={title}
              onChange={(e) => setTitle(e.target.value)}
              onBlur={() => setShowInputEle(false)}
              autoFocus
            />
          ) : (
            <>
              <Typography variant="h2" sx={{ fontSize: '25px' }} onDoubleClick={handleDoubleClick}>
                {title}
              </Typography>
             <a onDoubleClick={handleDoubleClick} > <img alt="edit-sign" src={editlogo} style={{ marginLeft: '11px', marginTop: '5px' }} /></a>
            </>
          )}
        </Box>
      </Box>
    
    
      <Button>+ addsection</Button> */}
        </>
     );
}
 
export default Duplicate;